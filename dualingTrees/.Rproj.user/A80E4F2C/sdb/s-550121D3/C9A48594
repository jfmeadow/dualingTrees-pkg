{
    "collab_server" : "",
    "contents" : "\nWill become pair_trees.R\nMight just go to plotting function. Is middle step necessary? \n\ninput: \n\n* take list object from input_trees\n* take colors for \n  * trees, \n  * split node in trees\n\n\n\n\n\n\n\n\n\nSTOPPED HERE. ASSIGN COLORS TO X AND Y. \n\n\nLittle lookup table for positive or negative values. \n\n```{r colors}\ncol2 <- c('#4daf4a', '#B351A1', '#5B1A18')\n\n\n\nwes.zis <- wes_palette('Zissou', n=100, type = 'continuous')\n\n# colsPN <- data.frame(cols=c('#e41a1c', '#377eb8', 'transparent'), \n#                      values=c(-1, 1, 0))\ncolsPN <- data.frame(cols=c(wes.zis[1], wes.zis[length(wes.zis)], 'transparent'), \n                     values=c(1, -1, 0))\n```\n\n\n\n```{r setFungalColors}\nfCol <- rep(0, ncol(mat))\n\ni <- 1\nfor(i in 1:ncol(mat)) {\n\tsp <- colnames(mat)[i]\n\tthese <- which(datTrim$fungalName == sp)\n\tif(all(datTrim$AMsub[these] == 'YES')) {\n    fCol[i] <- 1\n    }\n  if(all(datTrim$EMsub[these] == 'YES')) {\n    fCol[i] <- 2\n    }\n  \n  }\n```\n\t\t\n\n```{r setPlantColors}\n## Assign plants to AM(1) EM(2) or both(3)\npCol <- rep(0, nrow(mat))\nfor(i in 1:nrow(mat)) {\n\tsp <- rownames(mat)[i]\n\tthese <- which(datTrim$plantName == sp)\n  if(all(datTrim$AMsub[these] == 'YES')) {\n    pCol[i] <- 1\n    }\n  if(all(datTrim$EMsub[these] == 'YES')) {\n    pCol[i] <- 2\n    }\n  if(any(datTrim$AMsub[these] == 'YES') && any(datTrim$EMsub[these] == 'YES')) {\n    pCol[i] <- 3\n    }\n\t}\n# pCol\n```\n\n\n\n```{r opaqueColors}\ncolAM <- col2[1]\ncolEM <- col2[2]\ncolBoth <- col2[3]\n```\n\n\n\n```{r fungalBranchColors}\nnames(f.phylo)\nf.phylo$Nnode\nlength(f.phylo$edge)\nfBrCol <- rep('gray40', 188)\nfBrCol[1:65] <- colEM\nfBrCol[66:94] <- colAM \n```\n\n\n\n```{r offsetPlantNames}\nplantsHighlightIndex <- which(names(pSums) != '')\nplantsHighlight <- pSums[which(names(pSums) != '')]\nplantsHighlightIndex %>% length\ncbind(pn20, 1:44)\nplHiY.adjust <- c( \n  0,1, 0, 1,           # 1-4\n  0, 0, 0, -.5,        # 5-8\n  0, 1, 0, 0,          # 11-12\n  0, 0, -1, 0,         # 13-16\n  .5, -1, .4, .4,     # 17-20\n  0, 0, 0, -.4,          # 21-24\n  0, 0, 0, 0,          # 25-28\n  0, 0, 0, 0,          # 29-32\n  -.2, -.8, .2, .4,    # 33-36\n  0, -.8, -.6, .8,     # 37-40\n  0, 0, 0, 0           # 41-44\n  )\nplHiY.adjust %>% length\nplHiY <- plantsHighlightIndex + plHiY.adjust\n\npn20\nplHiY %>% length\npn20 %>% length\n# plHiY <- abs(plantsHighlightIndex)\n```\n\n\n\n```{r}\n# setwd('/Users/jamesmeadow/Dropbox (Personal)/MycoDB_DataPaper/Rmarkdown/dualingTrees_package_working_files/working')\n# save.image('WORKING_effect_size_figures.RData')\n# load('WORKING_effect_size_figures.RData')\n# load('WORKING_PLOT_TREES.RData')\nls()\n```\n\n\n\n",
    "created" : 1494476466208.000,
    "dirty" : false,
    "encoding" : "UTF-8",
    "folds" : "",
    "hash" : "3938883590",
    "id" : "C9A48594",
    "lastKnownWriteTime" : 1494476802,
    "last_content_update" : 1494476802668,
    "path" : "~/Dropbox (Personal)/MycoDB_DataPaper/Rmarkdown/dualingTrees_package_working_files/working/SETUP_FIG_MYCODB.Rmd",
    "project_path" : null,
    "properties" : {
    },
    "relative_order" : 13,
    "source_on_save" : false,
    "source_window" : "",
    "type" : "r_markdown"
}